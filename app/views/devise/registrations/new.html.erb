<div class="container p-t-1">
  <div class="row">
    <div class="col-lg-6 col-lg-offset-3">
      <h2><%= t "signup" %></h2>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :email, label: t("email"), required: true, autofocus: true %>
          <%= f.input :password, label: t("password"), required: true, hint: (t("minimum_password_length", length: @minimum_password_length.to_s) if @minimum_password_length) %>
          <%= f.input :password_confirmation, label: t("password_confirmation"), required: true %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, t("signup"), class: "btn btn-primary" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
